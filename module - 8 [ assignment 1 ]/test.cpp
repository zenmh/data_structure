#include <bits/stdc++.h>
using namespace std;

int main()
{
      /* cout << endl
          << "Address of tmp's next is before delete - " << tmp->next << endl;

                  cout << endl
          << "Address of tail's next is before assign - " << tail->next << endl;

             cout << endl
          << "Now tail is - " << tail->val << endl
          << "Address of tail's next is after assign - " << tail->next << endl
          << "Address of tmp's next is after delete - " << tmp->next << endl; */

      return 0;
}